---
phase: 03-integration-testing-go-live
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [COMPAT-01, COMPAT-02, COMPAT-03, COMPAT-04]

must_haves:
  truths:
    - "Client receives invite email with onboarding link"
    - "Clicking link opens web chat connected to Scout agent"
    - "Scout greets warmly and conducts 5-phase discovery conversation"
    - "SSE streaming delivers responses in real time without interruption"
    - "Client can close browser and resume conversation from same link"
    - "Final message contains JSON block matching OnboardingWebsiteData schema"
    - "Final message contains ONBOARDING_COMPLETE sentinel"
    - "Markdown brief appears in briefs/ directory with correct naming"
  artifacts:
    - path: "~/.openclaw/workspace-client-onboarding/briefs/"
      provides: "Generated markdown brief from test session"
      contains: "YYYY-MM-DD-*.md file"
  key_links:
    - from: "web app admin dashboard"
      to: "Scout agent via gateway"
      via: "x-openclaw-agent-id header through cloudflare tunnel"
      pattern: "SSE streaming response"
    - from: "Scout final message"
      to: "web app extraction"
      via: "JSON block in assistant message"
      pattern: "OnboardingWebsiteData schema fields"
    - from: "user:token request field"
      to: "on-disk session key"
      via: "agent:client-onboarding:responses-user:<token>"
      pattern: "30-day session persistence"
---

<objective>
Verify the complete Scout onboarding flow works end-to-end through live infrastructure.

Purpose: Confirm that everything built in Phases 1 and 2 works together when a real client walks through the experience. This is the final gate before Scout goes live.

Output: Verified working flow (no code artifacts — this is a testing phase)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-integration-testing-go-live/03-CONTEXT.md
@.planning/phases/01-agent-infrastructure/01-02-SUMMARY.md
@.planning/phases/02-discovery-conversation-system/02-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: SSE Streaming and Gateway Connectivity</name>
  <files>No files modified — human verification only</files>
  <read_first>
    - .planning/phases/01-agent-infrastructure/01-02-SUMMARY.md (routing header setup)
    - .planning/research/AGENT-SETUP.md §3 (HTTP routing via x-openclaw-agent-id)
  </read_first>
  <what-built>Phases 1 and 2 configured Scout agent with routing header, gateway connectivity, and SSE streaming through cloudflare tunnel.</what-built>
  <action>
    Randall performs manual verification of SSE streaming and gateway connectivity:
    1. Open the admin dashboard for gannsystems.pro and send a test invite to your own email address
    2. Check your inbox for the invite email — confirm it arrives with the onboarding link
    3. Click the onboarding link — it should open the web chat interface
    4. Open browser DevTools > Console before sending any messages
    5. Type a greeting (e.g., "Hi there") and send it
    6. Watch the response stream in — it should appear word-by-word via SSE (not all at once after a delay)
    7. Verify the response comes from Scout (warm greeting, introduces itself, sets expectations about the discovery process)

    What to look for:
    - SSE streaming works smoothly (no choppy delivery, no connection drops) — verifies COMPAT-01
    - Response is clearly from Scout agent, not the default agent
    - No errors in browser console related to streaming or authentication
    - Token generation and validation work without changes — verifies COMPAT-03

  </action>
  <how-to-verify>
    Follow the action steps above. Report whether SSE streaming delivers responses progressively and whether Scout responds (not the default agent).
  </how-to-verify>
  <verify>Human confirms SSE streaming works and Scout agent responds</verify>
  <acceptance_criteria>
    - Invite email received with working onboarding link
    - Web chat connects to Scout agent (not default agent)
    - SSE streaming delivers response progressively (word-by-word)
    - No connection errors through cloudflare tunnel
    - Browser console shows no errors related to streaming or authentication
  </acceptance_criteria>
  <done>SSE streaming works through cloudflare tunnel, Scout agent responds to web chat messages, no console errors</done>
  <resume-signal>Type "streaming works" if SSE and connectivity are good, or describe any issues observed</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Full Discovery Walkthrough and Output Verification</name>
  <files>~/.openclaw/workspace-client-onboarding/briefs/ (brief file created by agent)</files>
  <read_first>
    - .planning/phases/02-discovery-conversation-system/02-02-SUMMARY.md (AGENTS.md protocol details)
    - .planning/REQUIREMENTS.md §Discovery Framework and §Output (DISC-01 through OUT-05)
    - /Users/rgann/source/gannsystems.pro-landing-page/packages/types/src/onboarding.ts (OnboardingWebsiteData schema)
  </read_first>
  <what-built>Phase 2 wrote Scout's SOUL.md persona, BOOTSTRAP.md greeting, and AGENTS.md discovery protocol with 19 questions across 5 phases, brief generation, and JSON emission.</what-built>
  <action>
    Randall walks through the complete Scout discovery conversation as a mock client:
    1. Continue the conversation from Task 1 (or start fresh if needed)
    2. Play the role of a mock client — use a realistic scenario:
       - Business: "Sweet Sunrise Bakery" (or similar local business)
       - Industry: food/bakery
       - Provide mix of clear answers, vague answers, and a few "I'm not sure" responses
    3. Walk through the ENTIRE discovery conversation. Watch for:
       - 5-phase flow: warm-up, business context, design direction, site structure, wrap-up
       - One question at a time (never multi-question dumps)
       - Warm acknowledgment of every answer before the next question
       - Natural transitions between phases with progress indicators
       - IDK handling: when you say "I'm not sure", Scout should offer examples or suggest defaults
       - Adaptive probing: follow-up questions based on your specific answers
    4. Complete the conversation through to the wrap-up phase
    5. Verify the final message contains:
       - A summary of what was discussed
       - A JSON block with OnboardingWebsiteData fields (businessName, industry, targetAudience, etc.)
       - The ONBOARDING_COMPLETE sentinel text
       - Null values for any fields you said "I don't know" to (not guessed values)
    6. After completion, check the briefs directory:
       ls ~/.openclaw/workspace-client-onboarding/briefs/
       Verify a markdown file exists with format YYYY-MM-DD-sweet-sunrise-bakery.md (or similar slug)
    7. Read the brief and confirm it contains your actual answers, not hallucinated content

    This verifies: D-02 pass criteria, COMPAT-04 (JSON block is primary data path, brief is bonus)

  </action>
  <how-to-verify>
    Follow the action steps above. Report on conversation quality, JSON block contents, ONBOARDING_COMPLETE presence, and brief file generation.
  </how-to-verify>
  <verify>Human confirms discovery conversation completes with valid JSON output and brief file</verify>
  <acceptance_criteria>
    - Scout follows 5-phase discovery flow from warm-up through wrap-up
    - One question per message, warm acknowledgments, natural transitions
    - IDK responses handled with examples/defaults (not blocked)
    - Final message contains valid JSON block with OnboardingWebsiteData fields
    - Final message contains ONBOARDING_COMPLETE sentinel
    - Null fields for unanswered questions (no hallucination)
    - Markdown brief file exists in briefs/ with correct date-slug naming
    - Brief content matches actual conversation (not fabricated)
  </acceptance_criteria>
  <done>Full discovery conversation completed, JSON block emitted with correct schema, ONBOARDING_COMPLETE sentinel present, markdown brief written to briefs/ directory</done>
  <resume-signal>Type "discovery complete" if the full flow works, or describe issues with specific phases/outputs</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Session Continuity Test</name>
  <files>No files modified — human verification only</files>
  <read_first>
    - .planning/phases/03-integration-testing-go-live/03-CONTEXT.md §Session Continuity (D-03, D-04, D-05)
    - .planning/research/AGENT-SETUP.md (session management via user:token)
  </read_first>
  <what-built>Session continuity relies on user:token field in request body, mapped to on-disk session key (agent:client-onboarding:responses-user:TOKEN) with 30-day persistence. This was configured in Phase 1.</what-built>
  <action>
    Randall tests session continuity by interrupting and resuming a conversation:
    1. Start a NEW onboarding session (send yourself another test invite, or use a different token URL)
    2. Answer 3-4 of Scout's questions (get partway through the discovery)
    3. Close the browser tab completely (not just minimize — fully close it)
    4. Wait at least 30 seconds
    5. Reopen the same onboarding link (from the invite email)
    6. Send a message like "Hi, I'm back" or "I was here earlier"
    7. Verify Scout recognizes the returning session:
       - Should NOT restart the discovery from scratch
       - Should acknowledge the return and pick up where you left off
       - Previous conversation context should be intact
    8. Complete this session through to the end if desired (optional — the resume verification is the key test)

    What to look for:
    - Same token URL produces the same session (verifies COMPAT-02)
    - No "nice to meet you" restart — Scout knows you were already talking
    - Previous answers are remembered in the conversation context
    - No token validation errors (verifies COMPAT-03)

    Per D-05: The 30-minute previous_response_id TTL is NOT a concern here because the web app uses the user:token field which has a 30-day on-disk session key.

  </action>
  <how-to-verify>
    Follow the action steps above. Report whether the conversation resumed where it left off or restarted from scratch.
  </how-to-verify>
  <verify>Human confirms session resume works after closing and reopening browser</verify>
  <acceptance_criteria>
    - Reopening the same onboarding link resumes the existing conversation
    - Scout does not restart discovery from the beginning
    - Previous conversation context (answers given) is preserved
    - No authentication or token validation errors on resume
    - Session continuity works via user:token field (not previous_response_id)
  </acceptance_criteria>
  <done>Session continuity verified — closing browser and reopening same link resumes conversation with full context</done>
  <resume-signal>Type "session resume works" if continuity is verified, or describe what happened when you reopened the link</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Description                                  |
| ----------------------------- | -------------------------------------------- |
| client browser to web app API | Onboarding token in URL is the auth boundary |
| web app API to gateway        | x-openclaw-agent-id header routes to Scout   |
| gateway to Scout agent        | Agent processes client input                 |

## STRIDE Threat Register

| Threat ID | Category               | Component                  | Disposition | Mitigation Plan                                                           |
| --------- | ---------------------- | -------------------------- | ----------- | ------------------------------------------------------------------------- |
| T-03-01   | Spoofing               | onboarding token URL       | accept      | Tokens are single-use invites; low-value target for v1 with known clients |
| T-03-02   | Information Disclosure | briefs/ directory          | accept      | Server-side only, not exposed to web; Randall is sole operator            |
| T-03-03   | Tampering              | x-openclaw-agent-id header | accept      | Header set server-side in web app API route, not client-controllable      |

</threat_model>

<verification>
All three checkpoint tasks must pass:
1. SSE streaming works through cloudflare tunnel without drops
2. Full discovery conversation completes with valid JSON output and brief generation
3. Session resume works after closing and reopening browser

Per D-08: Once all three pass, Scout is ready for real clients. No additional go-live steps needed.
Per D-09: Fallback path (email Q&A with 10 questions) remains available if issues arise post-launch.
</verification>

<success_criteria>

- Scout delivers a complete, working onboarding experience through the live web app
- SSE streaming is smooth and reliable through cloudflare tunnel
- Session continuity allows clients to leave and return
- JSON block and markdown brief are both generated correctly
- No regressions to existing infrastructure
  </success_criteria>

<output>
After completion, create `.planning/phases/03-integration-testing-go-live/03-01-SUMMARY.md`
</output>
