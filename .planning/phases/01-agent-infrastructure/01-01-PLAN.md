---
phase: 01-agent-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.openclaw/openclaw.json
  - ~/.openclaw/workspace-client-onboarding/IDENTITY.md
  - ~/.openclaw/workspace-client-onboarding/USER.md
  - ~/.openclaw/workspace-client-onboarding/TOOLS.md
  - ~/.openclaw/agents/client-onboarding/agent/auth-profiles.json
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03

must_haves:
  truths:
    - "openclaw agents list shows client-onboarding agent"
    - "Agent has identity name Scout and emoji"
    - "Agent workspace exists with briefs/ and memory/ directories"
    - "Agent has working auth profiles copied from main agent"
  artifacts:
    - path: "~/.openclaw/workspace-client-onboarding/IDENTITY.md"
      provides: "Agent identity definition"
      contains: "Scout"
    - path: "~/.openclaw/workspace-client-onboarding/USER.md"
      provides: "Owner context for agent"
      contains: "GannSystems"
    - path: "~/.openclaw/workspace-client-onboarding/TOOLS.md"
      provides: "Environment notes for agent"
      contains: "briefs/"
    - path: "~/.openclaw/agents/client-onboarding/agent/auth-profiles.json"
      provides: "API key auth for agent"
  key_links:
    - from: "~/.openclaw/openclaw.json"
      to: "~/.openclaw/workspace-client-onboarding/"
      via: "agents.list[].workspace path"
      pattern: "workspace-client-onboarding"
    - from: "~/.openclaw/openclaw.json"
      to: "~/.openclaw/agents/client-onboarding/agent/"
      via: "agents.list[].agentDir path"
      pattern: "agents/client-onboarding"
---

<objective>
Create the client-onboarding agent with workspace, identity, and auth configuration.

Purpose: Establish the agent infrastructure so it exists in the openclaw system, has an isolated workspace, can authenticate with LLM providers, and has seed workspace files.
Output: A registered openclaw agent named "Scout" with workspace directories, identity, user context, and working auth.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/AGENT-SETUP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create agent and workspace directories</name>
  <files>~/.openclaw/openclaw.json, ~/.openclaw/workspace-client-onboarding/</files>
  <read_first>
    - ~/.openclaw/openclaw.json (to see existing agents config before modification)
    - .planning/research/AGENT-SETUP.md (agent creation command and expected output)
  </read_first>
  <action>
Run the agent creation command:

```bash
openclaw agents add client-onboarding \
  --workspace ~/.openclaw/workspace-client-onboarding \
  --non-interactive
```

This creates the agent entry in openclaw.json, the agentDir at `~/.openclaw/agents/client-onboarding/agent/`, and the session store.

Then create the workspace subdirectories:

```bash
mkdir -p ~/.openclaw/workspace-client-onboarding/memory
mkdir -p ~/.openclaw/workspace-client-onboarding/briefs
```

Verify the agent was created:

```bash
openclaw agents list
```

The output must show `client-onboarding` in the list.
</action>
<verify>
<automated>openclaw agents list 2>&1 | grep -q "client-onboarding" && echo "PASS: agent exists" || echo "FAIL: agent not found"</automated>
</verify>
<acceptance_criteria> - `openclaw agents list` output contains "client-onboarding" - Directory exists: `~/.openclaw/workspace-client-onboarding/memory/` - Directory exists: `~/.openclaw/workspace-client-onboarding/briefs/` - Directory exists: `~/.openclaw/agents/client-onboarding/agent/` - `~/.openclaw/openclaw.json` contains `"client-onboarding"` in agents.list
</acceptance_criteria>
<done>Agent registered in openclaw, workspace directories created, agent appears in agents list</done>
</task>

<task type="auto">
  <name>Task 2: Configure identity, workspace files, and auth</name>
  <files>
    ~/.openclaw/workspace-client-onboarding/IDENTITY.md
    ~/.openclaw/workspace-client-onboarding/USER.md
    ~/.openclaw/workspace-client-onboarding/TOOLS.md
    ~/.openclaw/agents/client-onboarding/agent/auth-profiles.json
  </files>
  <read_first>
    - ~/.openclaw/agents/main/agent/auth-profiles.json (source auth to copy)
    - .planning/research/AGENT-SETUP.md (workspace file patterns and identity command)
  </read_first>
  <action>
**Step A: Set agent identity via CLI**

```bash
openclaw agents set-identity --agent client-onboarding --name "Scout" --emoji "🔍"
```

**Step B: Create IDENTITY.md**

Write to `~/.openclaw/workspace-client-onboarding/IDENTITY.md`:

```markdown
# IDENTITY.md - Who Am I?

- **Name:** Scout
- **Creature:** Client discovery agent -- a website needs detective
- **Vibe:** Curious, thorough, consultative. Asks smart questions and captures everything.
- **Emoji:** 🔍
- **Avatar:** _(none yet)_

---

Scout: uncovering what clients really need, one conversation at a time.
```

**Step C: Create USER.md**

Write to `~/.openclaw/workspace-client-onboarding/USER.md`:

```markdown
# USER.md - Owner Context

- **Owner:** Randall Gann
- **Company:** GannSystems.Pro
- **Role:** Web development consultant and freelancer
- **Website:** https://gannsystems.pro
- **Focus:** Building websites for small businesses and entrepreneurs
- **Location:** United States

## What Scout Does for Randall

Scout conducts discovery conversations with prospective clients who are interested in having a website built. The goal is to gather enough information about their business, goals, design preferences, and site requirements to produce a comprehensive project brief that Randall can use to create a proposal and begin design work.

## How Clients Arrive

Clients receive an email invitation with a unique chat link. They click through to a web chat interface on gannsystems.pro where they interact with Scout. Each client has a unique session token.
```

**Step D: Create TOOLS.md**

Write to `~/.openclaw/workspace-client-onboarding/TOOLS.md`:

```markdown
# TOOLS.md - Environment and Resources

## Available Tools

- **write** -- Create files in the workspace (used for briefs and memory)
- **edit** -- Edit existing workspace files
- **read** -- Read workspace files

## Key Directories

- `briefs/` -- Client discovery briefs (output)
- `memory/` -- Session memory and notes

## References

- GannSystems.Pro website: https://gannsystems.pro
- Client briefs are written as Markdown files in the `briefs/` directory
```

**Step E: Copy auth profiles**

```bash
cp ~/.openclaw/agents/main/agent/auth-profiles.json \
   ~/.openclaw/agents/client-onboarding/agent/auth-profiles.json
```

This gives the new agent access to the same LLM API keys as the main agent.
</action>
<verify>
<automated>test -f ~/.openclaw/workspace-client-onboarding/IDENTITY.md && grep -q "Scout" ~/.openclaw/workspace-client-onboarding/IDENTITY.md && test -f ~/.openclaw/workspace-client-onboarding/USER.md && grep -q "GannSystems" ~/.openclaw/workspace-client-onboarding/USER.md && test -f ~/.openclaw/workspace-client-onboarding/TOOLS.md && grep -q "briefs/" ~/.openclaw/workspace-client-onboarding/TOOLS.md && test -f ~/.openclaw/agents/client-onboarding/agent/auth-profiles.json && echo "PASS: all files created" || echo "FAIL: missing files"</automated>
</verify>
<acceptance_criteria> - `~/.openclaw/workspace-client-onboarding/IDENTITY.md` exists and contains "Scout" and "🔍" - `~/.openclaw/workspace-client-onboarding/USER.md` exists and contains "GannSystems" and "Randall Gann" - `~/.openclaw/workspace-client-onboarding/TOOLS.md` exists and contains "briefs/" and "memory/" - `~/.openclaw/agents/client-onboarding/agent/auth-profiles.json` exists and is valid JSON - `openclaw agents set-identity` command completed without error
</acceptance_criteria>
<done>Agent has identity (Scout, 🔍), workspace files (IDENTITY.md, USER.md, TOOLS.md), and auth profiles copied from main agent</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary      | Description                                            |
| ------------- | ------------------------------------------------------ |
| Auth profiles | API keys copied between agent directories on same host |

## STRIDE Threat Register

| Threat ID | Category                   | Component               | Disposition | Mitigation Plan                                                                                                   |
| --------- | -------------------------- | ----------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------- |
| T-01-01   | I (Information Disclosure) | auth-profiles.json copy | accept      | Same host, same user, same filesystem permissions. Keys are already accessible to the main agent on this machine. |

</threat_model>

<verification>
- `openclaw agents list` shows client-onboarding
- All three workspace .md files exist with correct content
- Auth profiles JSON is valid and exists at the agent path
- Workspace has briefs/ and memory/ subdirectories
</verification>

<success_criteria>

- Agent `client-onboarding` is registered and visible in openclaw agents list
- Workspace at `~/.openclaw/workspace-client-onboarding/` has IDENTITY.md, USER.md, TOOLS.md, briefs/, memory/
- Agent identity is "Scout" with 🔍 emoji
- Auth profiles are copied and the agent can authenticate with LLM providers
  </success_criteria>

<output>
After completion, create `.planning/phases/01-agent-infrastructure/01-01-SUMMARY.md`
</output>
