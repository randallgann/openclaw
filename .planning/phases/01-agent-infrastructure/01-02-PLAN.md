---
phase: 01-agent-infrastructure
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - /Users/rgann/source/gannsystems.pro-landing-page/apps/web/app/api/chat/[token]/message/route.ts
autonomous: false
requirements:
  - INFRA-04
  - INFRA-05

must_haves:
  truths:
    - "Web app sends x-openclaw-agent-id header with value client-onboarding"
    - "Gateway responds to requests routed to client-onboarding agent"
    - "Curl test to gateway with agent header returns a valid response"
  artifacts:
    - path: "/Users/rgann/source/gannsystems.pro-landing-page/apps/web/app/api/chat/[token]/message/route.ts"
      provides: "HTTP routing header for agent selection"
      contains: "x-openclaw-agent-id"
  key_links:
    - from: "/Users/rgann/source/gannsystems.pro-landing-page/apps/web/app/api/chat/[token]/message/route.ts"
      to: "gateway /v1/responses"
      via: "x-openclaw-agent-id header in fetch call"
      pattern: "x-openclaw-agent-id.*client-onboarding"
---

<objective>
Add agent routing header to the web app and verify end-to-end gateway connectivity.

Purpose: Route web chat traffic to the new client-onboarding agent instead of the default agent, then verify the full path works.
Output: Web app routes to correct agent; gateway responds successfully to agent-targeted requests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/AGENT-SETUP.md
@.planning/phases/01-agent-infrastructure/01-01-SUMMARY.md
</context>

<interfaces>
<!-- Current fetch call in the web app route.ts (lines 179-193) -->
From /Users/rgann/source/gannsystems.pro-landing-page/apps/web/app/api/chat/[token]/message/route.ts:
```typescript
openclawRes = await fetch(`${gatewayUrl}/v1/responses`, {
  method: "POST",
  headers: {
    "Content-Type": "application/json",
    Authorization: `Bearer ${gatewayToken}`,
    "x-openclaw-scopes": "operator.read,operator.write",
  },
  body: JSON.stringify({
    model: "openclaw",
    input: message,
    stream: true,
    user: token,
  }),
});
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add x-openclaw-agent-id header to web app fetch call</name>
  <files>/Users/rgann/source/gannsystems.pro-landing-page/apps/web/app/api/chat/[token]/message/route.ts</files>
  <read_first>
    - /Users/rgann/source/gannsystems.pro-landing-page/apps/web/app/api/chat/[token]/message/route.ts (full file to see current headers block)
  </read_first>
  <action>
Edit the fetch call in `/Users/rgann/source/gannsystems.pro-landing-page/apps/web/app/api/chat/[token]/message/route.ts` (around line 181-185).

Add one line to the headers object: `"x-openclaw-agent-id": "client-onboarding",`

The headers block should become:

```typescript
headers: {
  "Content-Type": "application/json",
  Authorization: `Bearer ${gatewayToken}`,
  "x-openclaw-scopes": "operator.read,operator.write",
  "x-openclaw-agent-id": "client-onboarding",
},
```

This single header addition tells the openclaw gateway to route this request to the client-onboarding agent instead of the default agent. No other changes to the file are needed.
</action>
<verify>
<automated>grep -q 'x-openclaw-agent-id.\*client-onboarding' "/Users/rgann/source/gannsystems.pro-landing-page/apps/web/app/api/chat/[token]/message/route.ts" && echo "PASS: header present" || echo "FAIL: header missing"</automated>
</verify>
<acceptance_criteria> - File contains the string `"x-openclaw-agent-id": "client-onboarding"` in the headers object of the fetch call - The header is inside the `headers: { ... }` block alongside Content-Type, Authorization, and x-openclaw-scopes - No other lines in the file were changed
</acceptance_criteria>
<done>Web app fetch call includes x-openclaw-agent-id: client-onboarding header for agent routing</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Restart gateway and verify end-to-end connectivity</name>
  <read_first>
    - .planning/research/AGENT-SETUP.md (restart and test commands in Section 7, Steps 7-8)
  </read_first>
  <action>
**Step A: Restart the gateway to pick up agent config changes**

```bash
openclaw gateway restart
```

Wait for the gateway to come back up (check with `ss -ltnp | grep 18789` or `openclaw channels status --probe`).

**Step B: Verify agent is listed**

```bash
openclaw agents list
```

Confirm `client-onboarding` appears with name "Scout".

**Step C: Test gateway connectivity with curl**

```bash
curl -s -X POST http://localhost:18789/v1/responses \
  -H "Content-Type: application/json" \
  -H "x-openclaw-agent-id: client-onboarding" \
  -d '{"model":"openclaw","input":"Hello, I am testing connectivity","stream":false}'
```

The response should be a JSON object with a text response from the agent (not an error). If auth is required, add `-H "Authorization: Bearer <gateway-token>"` (get the token from the web app's environment or `~/.openclaw/openclaw.json`).

**Step D: Verify agent isolation**

The response should come from the client-onboarding agent (Scout), not the default/main agent. The content should reflect the agent's identity if IDENTITY.md was loaded.
</action>
<what-built>Agent infrastructure: registered agent, workspace files, auth, web app routing header, gateway restart</what-built>
<how-to-verify> 1. Run `openclaw agents list` -- confirm client-onboarding/Scout appears 2. Run the curl command above -- confirm a JSON response (not an error) 3. Check the response content reflects the Scout agent identity 4. Optionally: test via the web app chat interface to confirm routing works end-to-end through the cloudflare tunnel
</how-to-verify>
<resume-signal>Type "approved" if curl returns a valid response from Scout, or describe any errors</resume-signal>
<verify>
<automated>openclaw agents list 2>&1 | grep -q "client-onboarding" && echo "PASS: agent listed" || echo "FAIL: agent not listed"</automated>
</verify>
<acceptance_criteria> - `openclaw agents list` shows client-onboarding with name Scout - curl to localhost:18789 with x-openclaw-agent-id: client-onboarding returns HTTP 200 with JSON response body - Response body contains agent-generated text (not an error message) - Gateway is running (port 18789 is listening)
</acceptance_criteria>
<done>Gateway restarted, agent responds to routed requests, end-to-end connectivity confirmed</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary           | Description                                                                        |
| ------------------ | ---------------------------------------------------------------------------------- |
| Web app -> Gateway | HTTP request crosses from Next.js server to openclaw gateway via cloudflare tunnel |

## STRIDE Threat Register

| Threat ID | Category      | Component                  | Disposition | Mitigation Plan                                                                                                                              |
| --------- | ------------- | -------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| T-01-02   | S (Spoofing)  | x-openclaw-agent-id header | accept      | Header is sent server-side from the Next.js API route, not from the client browser. The gateway already requires Authorization bearer token. |
| T-01-03   | T (Tampering) | agent-id header value      | accept      | Hardcoded in server-side code, not user-controllable. Client cannot inject or override this header.                                          |

</threat_model>

<verification>
- Web app route.ts contains the x-openclaw-agent-id header
- Gateway is running and accepting requests
- curl with agent header returns a valid response
- Agent responds with Scout identity
</verification>

<success_criteria>

- The header `x-openclaw-agent-id: client-onboarding` is present in the web app's gateway fetch call
- Gateway restart completed successfully
- curl test to gateway with the agent header returns a non-error JSON response
- Human confirms the response comes from the Scout agent
  </success_criteria>

<output>
After completion, create `.planning/phases/01-agent-infrastructure/01-02-SUMMARY.md`
</output>
